pipeline {
    agent any
    
    environment {
        WORKSPACE_PATH = '/Users/koti/demo-time/ci-cd-chaos-workshop'
    }
    
    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out code for Secrets Management'
                checkout scm
            }
        }
        
        stage('Build') {
            steps {
                echo 'Building Docker image for Secrets Management'
                script {
                    def image = docker.build("jenkins-workshop-${env.BUILD_NUMBER}")
                }
            }
        }
        
        stage('Test') {
            steps {
                echo 'Running tests for Secrets Management'
                sh 'python -m pytest tests/ -v'
            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Deploying Secrets Management'
                // Add deployment steps here
            }
        }
    }
    
    post {
        always {
            echo 'Pipeline completed for Secrets Management'
        }
        success {
            echo 'Pipeline succeeded for Secrets Management'
        }
        failure {
            echo 'Pipeline failed for Secrets Management'
        }
    }
}